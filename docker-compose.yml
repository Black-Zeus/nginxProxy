services:
  nginx:
    image: nginx:1.27-alpine
    container_name: nginx-proxy
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./html:/usr/share/nginx/html:ro
    networks:
      - proxy
      - itop
      - itophub
      - n8n
      - talos

networks:
  proxy:
    driver: bridge
  itop:
    external: true
    name: itop_default
  itophub:
    external: true
    name: itophub_internal
  n8n:
    external: true
    name: n8n_network
  talos:
    external: true
    name: talos_default
